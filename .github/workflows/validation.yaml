name: Validation

on: 
  pull_request:
    branches:
    - main

jobs:
  pages:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Split the files 
      run: |
        tools/setup-pages.sh
